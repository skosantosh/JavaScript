// expressions
3+4
1991
true && false && !false

// Statement
if (23 > 10){
    const str = `23 is bigger`;
}

console.log(`John is ${50 - 30}`)//we can not put statement in curly bracket it will nat work only take expression.

ageOfraba = 18;
// this this way we can write condition inside
console.log(`I like to ${ageOfraba>=18? 'smoke🚬': 'not smoke🚭'}.`)