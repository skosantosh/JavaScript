const ageOfrab = 18;

ageOfrab >= 18 ? console.log (`You can smoke now 🚬`):
console.log(`Not allow to smoke`);

ageOfrab >= 18 ? console.log('Smoke🚬') : 'No smoke 🚭';

// this this way we can write condition inside
console.log(`I like to ${ageOfrab>=18? 'smoke🚬': 'not smoke🚭'}.`)